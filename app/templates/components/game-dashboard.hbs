{{#paper-card as |card|}}
  {{#paper-toolbar}}
    {{#paper-toolbar-tools}}
      <h2>{{game.name}}</h2>
    {{/paper-toolbar-tools}}
  {{/paper-toolbar}}
  {{#card.title as |title|}}
    {{#title.text as |text|}}
      {{#text.headline}}
        <span class="minutes">
          {{liquid-bind m class="animated-time"}}
        </span>
        <span class="seperator">
          {{#liquid-bind false class="animated-time"}}&#58;{{/liquid-bind}}
        </span>
        <span class="seconds">
          {{liquid-bind s class="animated-time"}}
        </span>
      {{/text.headline}}
      {{#text.subhead}}
        {{t "components.game-dashboard.round" round=1}}
      {{/text.subhead}}
    {{/title.text}}
  {{/card.title}}
  {{#card.actions}}
    {{#paper-button}}
      {{#paper-button warn=true onClick=(action "promptToEndGame")}}
        {{t "components.game-dashboard.end"}}
      {{/paper-button}}
    {{/paper-button}}
    {{#paper-button}}
      {{#paper-button raised=true primary=true}}
        {{t "components.game-dashboard.play"}}
      {{/paper-button}}
    {{/paper-button}}
  {{/card.actions}}
{{/paper-card}}

<div class="layout-row layout-xs-column">
  <div class="flex">
    {{#paper-card as |card|}}
      {{#paper-toolbar}}
        {{#paper-toolbar-tools}}
          <h2>{{t "components.game-dashboard.current"}}</h2>
        {{/paper-toolbar-tools}}
      {{/paper-toolbar}}
      {{#card.title as |title|}}
        {{#title.text as |text|}}
          {{#text.headline}}
            {{t "components.game-dashboard.blinds" small=5 big=10}}
          {{/text.headline}}
          {{#text.subhead}}
            {{t "components.game-dashboard.noAnte"}}
          {{/text.subhead}}
        {{/title.text}}
      {{/card.title}}
    {{/paper-card}}
  </div>

  <div class="flex">
    {{#paper-card as |card|}}
      {{#paper-toolbar class="secondary-toolbar"}}
        {{#paper-toolbar-tools}}
          <h2>{{t "components.game-dashboard.next"}}</h2>
        {{/paper-toolbar-tools}}
      {{/paper-toolbar}}
      {{#card.title as |title|}}
        {{#title.text as |text|}}
          {{#text.headline}}
            {{t "components.game-dashboard.blinds" small=10 big=20}}
          {{/text.headline}}
          {{#text.subhead}}
            {{t "components.game-dashboard.ante" ante=1}}
          {{/text.subhead}}
        {{/title.text}}
      {{/card.title}}
    {{/paper-card}}
  </div>

  <div class="flex">
    {{#paper-card as |card|}}
      {{#paper-toolbar class="secondary-toolbar"}}
        {{#paper-toolbar-tools}}
          <h2>{{t "components.game-dashboard.clock"}}</h2>
        {{/paper-toolbar-tools}}
      {{/paper-toolbar}}
      {{#card.title as |title|}}
        {{#title.text as |text|}}
          {{#text.headline}}
            {{! TODO HH:MM:SS }}
          {{/text.headline}}
          {{#text.subhead}}
            {{! TODO Started at HH:MM }}
          {{/text.subhead}}
        {{/title.text}}
      {{/card.title}}
    {{/paper-card}}
  </div>
</div>

<span id="game-dashboard-animate-modal-origin"></span>
{{#if showEndGameModal}}
  {{#paper-dialog
    fullscreen=false
    onClose=(action "endGame")
    openFrom="#game-dashboard-animate-modal-origin"
    closeTo="#game-dashboard-animate-modal-origin"
  }}

    {{#paper-toolbar}}
      {{#paper-toolbar-tools}}
        <h2>{{t "components.game-dashboard.modal.title"}}</h2>
        <span class="flex"></span>
        {{#paper-button iconButton=true onClick=(action "closeEndGameModal")}}
          {{paper-icon icon="close"}}
        {{/paper-button}}
      {{/paper-toolbar-tools}}
    {{/paper-toolbar}}

    {{#paper-dialog-content}}
      <p>{{t "components.game-dashboard.modal.description"}}</p>
    {{/paper-dialog-content}}

    {{#paper-dialog-actions class="layout-row"}}
      <span class="flex"></span>
      {{#paper-button onClick=(action "closeEndGameModal")}}
        {{t "components.game-dashboard.modal.cancel"}}
      {{/paper-button}}
      {{#paper-button onClick=(action "endGame")}}
        {{t "components.game-dashboard.modal.ok"}}
      {{/paper-button}}
    {{/paper-dialog-actions}}

  {{/paper-dialog}}
{{/if}}